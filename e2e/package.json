{
  "name": "@paperclip-pixel/e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Pixel Office Playwright e2e suite (PAPERCLIP_PIXELS-1 / SAA-231) driving the deployed bridge stack.",
  "type": "module",
  "scripts": {
    "test": "playwright test --config playwright.config.ts",
    "test:headed": "playwright test --config playwright.config.ts --headed",
    "install-browsers": "playwright install chromium",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.23.1",
    "pg": "^8.13.0",
    "typescript": "^5.9.3"
  }
}
